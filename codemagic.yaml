workflows:
  gtmobile-app:
      scripts:
      - name: Build Android APK
        working_directory: android
        script: sudo ./gradlew assembleRelease
      artifacts:
      - app/build/outputs/**/**/*.apk